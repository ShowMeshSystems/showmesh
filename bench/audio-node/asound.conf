# Userspace null ALSA device, so R6 exercises a real alsasink/ALSA API
# path with no /dev/snd and no host sound hardware. See README.md and R6's
# result for what this does and does not prove about a physical interface.
pcm.!default {
    type null
}
